<%= form_with(model: birthday, local: true) do |form| %>
  <% if birthday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(birthday.errors.count, "error") %> prohibited this birthday from being saved:</h2>

      <ul>
      <% birthday.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :birthday_name %>
  </div>

  <div class="field">
    <%= form.label :date_birthday %>
    <%= form.date_select :date_birthday, id: :birthday_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
